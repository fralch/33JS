const numbers = [1, 2, 3, 4, 5];
numbers.push(6); // agrega un elemento al final del array
numbers.pop(); // elimina el último elemento del array
console.log(numbers[0]); // imprime el primer elemento del array

 
